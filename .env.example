# SILAT ALTAWASUL - Environment variables
# Copy to .env and fill. No code changes needed for deploy.

# Database (PostgreSQL). Examples:
# Neon (free): postgresql://[user]:[password]@[host]/[dbname]?sslmode=require
#   Get it: https://neon.tech → Create project → Connection string (copy)
# Supabase:   postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres?sslmode=require
# Railway:     provided in dashboard
DATABASE_URL="postgresql://user:password@localhost:5432/silat"

# NextAuth. Generate: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-at-least-32-chars"
NEXTAUTH_URL="http://localhost:3000"

# Optional: file storage for historical reports (e.g. Vercel Blob)
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."

# Optional: to reset admin password when locked out, set a secret (8+ chars), then:
# POST /api/setup/reset-admin-password with body { "token": "<this-secret>", "newPassword": "newpass" }
# Remove or change this after use.
# ADMIN_RESET_SECRET="your-reset-secret"
